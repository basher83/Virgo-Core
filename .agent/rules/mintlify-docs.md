---
trigger: always_on
---

# Mintlify Documentation Rules

- Run `mint dev` to start the local web server preview
- Settings in `docs.json` apply globally to all pages
- Always refer to <https://mintlify.com/docs.json> for the offical docs.json scheme
- Always utilize mintlify mcp server tool `SearchMintlify` to fetch up to date offical docs
