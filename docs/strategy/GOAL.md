# Goal

Generate Ansible playbooks that achieve PASS scores on all six quality dimensions (idempotency, security,
module selection, error handling, structure, linting) for defined scenarios, delivered through a Claude Code plugin
that effectively leverages skills, commands, hooks, and agents where appropriate.
